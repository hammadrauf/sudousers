---
galaxy_info:
  author: Hammad Rauf <rauf.hammad@gmail.com>
  description: |
    Debian/RHEL creation of Super User with sudo permissions. Also sets the NOPASSWD
    flag so that Password is not asked on sudo usage.
  company: No Company
  license: MIT
  min_ansible_version: "2.1"
  role_name: sudousers
  namespace: hammadrauf
  platforms:
    - name: Fedora
      versions:
        - all
    - name: Debian
      versions:
        - all
  galaxy_tags:
    - superuser
    - sudo
    - linux
    - debian
    - rhel

dependencies: []